services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: marimo
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      # Coolify adds its runtime env_file to every Compose service. Explicitly
      # remove bot-only settings from PostgreSQL's container environment.
      DISCORD_TOKEN:
      WATER_XP:
      XP_WEBHOOK_URL:
      XP_WEBHOOK_TOKEN:
      LOG_LEVEL:
    ports:
      - "5433:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d marimo"]
      interval: 2s
      timeout: 2s
      retries: 20
    volumes:
      - marimo-db:/var/lib/postgresql/data

  bot:
    build: .
    depends_on:
      db:
        condition: service_healthy
    environment:
      DATABASE_URL: postgresql://postgres:postgres@db:5432/marimo
      DATABASE_REQUIRE_SSL: "false"
      DISCORD_TOKEN: ${DISCORD_TOKEN:?set DISCORD_TOKEN}
      WATER_XP: ${WATER_XP:-10}
      XP_WEBHOOK_URL: ${XP_WEBHOOK_URL:-}
      XP_WEBHOOK_TOKEN: ${XP_WEBHOOK_TOKEN:-}
      LOG_LEVEL: ${LOG_LEVEL:-info}
    restart: unless-stopped

volumes:
  marimo-db:
